= simple_form_for(@user) do |f|
  = f.error_notification

  .form-inputs
    = f.input :email
    = f.input :name
  .form-checkbox
    = f.association :groups, as: :check_boxes
  .form-actions
    = f.button :submit
